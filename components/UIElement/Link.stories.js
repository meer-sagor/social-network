import Link from './Link.vue';

export default {
  title: 'Link',
  components: Link
}
const Template = (args, { argTypes }) => ({})

export const Primary = Template.bind({});
Primary.args = {
    variant: 'primary',
  };
